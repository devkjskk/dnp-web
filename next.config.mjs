/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "nextjs-admin-web-git-main-make-it-down.vercel.app",
      "dnp.todly.io",
    ],
  },
};

export default nextConfig;
